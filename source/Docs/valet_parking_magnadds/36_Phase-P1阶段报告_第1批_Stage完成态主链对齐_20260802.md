# Phase-P1 阶段报告（第1批）

- 阶段：`Phase-P1`
- 批次：`第1批（启动批）`
- 报告日期：`2026-08-02`
- 目标分支：`DeepSeeK_V4_pro_20260731`
- 阶段状态：`IN_PROGRESS`

## 一、阶段目标

对齐 `IsReadyToFinishStage -> FinishScenario` 的关键行为语义，先完成一处高价值收敛：

- `direct release` 达到完成条件后，不再立即“重置后继续普通链路”，
- 改为锁存阶段完成态，进入 `FINISH_HOLD`，等待上层阶段切换。

## 二、输入与前置条件

- 输入文档：
  - `35_执行计划书_ValetParkingStageParking_阶段门禁与无缝交接_20260802.md`
  - `05_TempAPA原始流程复现差距对照表.md`
  - `14_原始流程差异收敛执行记录.md`
- 原始代码锚点：
  - `TempAPA_Code/planning/scenarios/valet_parking/stage_valet_parking_parking.cc`
- 当前工程代码：
  - `source/valet_parking/src/valet_parking_stage_parking_adapter.cpp`
  - `source/valet_parking/src/valet_parking_stage_finish_lite.*`

## 三、实施内容（本批）

1. 调整 direct release ready 后生命周期语义（代码改动）
   - 文件：`source/valet_parking/src/valet_parking_stage_parking_adapter.cpp`
   - 关键改动：
     - `runtime_lifecycle_event=direct_release_ready` 下，动作从
       `reset_planning_state_after_publish/reset_after_publish`
       调整为
       `latch_finish_hold_after_publish/keep_until_stage_reset/clear_released_command`。
     - `direct_finish_evaluation.ready_to_finish=true` 时：
       - 不再 `ResetPlanningState()`；
       - 改为 `direct_command.Reset(); stage_exit_requested=true;`。

2. 同步 smoke 断言到新语义（脚本改动）
   - 文件：`source/valet_parking_tools/smoke_valet_parking_x86.sh`
   - 关键改动：
     - `direct-forward-release` / `direct-backward-release` 的 runtime 断言由“reset清理”改为“finish锁存”。
     - 新增 `FINISH_HOLD` 分支出现断言。

3. 验证
   - x86 构建：通过。
   - m57 构建（本地 WSL）：仍受工具链环境限制，未在本批通过（符合既有 blocker）。
   - `direct-forward-release` smoke：
     - 首轮在旧断言下失败（预期内，因语义已变）；
     - 更新断言后复跑（含 pre-command warmup）通过行为观测，日志出现：
       - `STAGE_CONTROL DIRECT_FORWARD_RELEASED`
       - `STAGE_CONTROL FINISH_HOLD`

## 四、交付物清单（本批）

| 类型 | 文件 | 说明 |
|---|---|---|
| 代码 | `source/valet_parking/src/valet_parking_stage_parking_adapter.cpp` | direct release 完成态锁存 |
| 脚本 | `source/valet_parking_tools/smoke_valet_parking_x86.sh` | release 用例断言更新为 FINISH_HOLD 语义 |
| 文档 | `36_Phase-P1阶段报告_第1批_Stage完成态主链对齐_20260802.md` | 本报告 |
| 快照 | `status_snapshots/118_项目状态快照_Phase-P1_第1批_完成态锁存与release语义对齐.md` | 本批状态沉淀 |

## 五、验收结果（本批）

| 验收项 | 标准 | 结果 | 证据 |
|---|---|---|---|
| x86 编译 | `build_valet_parking.sh` 成功 | Pass | `out/valet_parking_phase_p1_118/.../x86/lib/libvalet_parking.so` |
| m57 编译 | 本机直编可过 | Fail(环境阻塞) | `m57_toolchain.cmake` 明确要求标准 Docker/工具链 |
| release 行为 | release -> finish hold 语义可观察 | Pass | smoke 日志含 `DIRECT_FORWARD_RELEASED` + `FINISH_HOLD` |
| 阶段结论 | 达到 Phase-P1 全量完成 | Not yet | 当前仅第1批，需继续 P1 其余收敛点 |

## 六、风险复盘

| 风险 | 是否发生 | 影响 | 对策 |
|---|---|---|---|
| 旧 smoke 断言与新语义不一致 | 是 | 首轮 smoke 失败 | 同步更新 release 断言后复跑 |
| release 场景时序抖动 | 是（轻微） | 首轮未稳定命中 | 增加 pre-command warmup 复跑 |

## 七、Git 记录

- 分支：`DeepSeeK_V4_pro_20260731`
- 当前状态：`未提交（Phase-P1 未收口）`
- 说明：按约定“每阶段完成后提交”；本批属于 Phase-P1 进行中，不触发阶段收口提交。

## 八、下一步建议（需审批）

- 继续执行 `Phase-P1 第2批`：
  1. 对齐普通链 `destination_reached + standstill` 到 `stage_exit_requested` 的一致性边界（含 fallback 分支）；
  2. 增补 `direct-backward-release` 的同口径验证与断言复跑；
  3. 做一次 `first-domain-id<=230` 的小批量 smoke 回归，确保不破坏已有链路。
- 继续前提：你确认继续 Phase-P1 第2批。
