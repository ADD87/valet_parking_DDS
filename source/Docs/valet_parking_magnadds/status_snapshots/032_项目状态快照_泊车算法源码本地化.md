# 项目状态快照 032：泊车算法源码本地化

- 快照编号：032
- 日期：2026-07-28
- 当前阶段：Fast-2 - `.so` + typed DDS + localized valet parking algorithm sources
- 阶段状态：PASS_ALGORITHM_LOCALIZATION_X86_M57_STATIC_BLOCKED_BOARD
- 创建原因：NEXT-031 后 PATH_PROVIDER MVP 边界已完成一轮安全验收；本阶段把当前已验证链路实际编译使用的 standalone 算法源码本地化到 `applications` 仓库，解除对外部 `E:\APA\DDS\parking_algorithm_standalone` 绝对路径的构建依赖。

---

## 1. 本次完成事项

- [x] 新增本地算法源码目录：
  - `applications/source/valet_parking/algorithm/parking_algorithm_standalone`
- [x] 复制当前 CMake 实际编译使用的 standalone 最小源码/头文件依赖：
  - 共 91 个文件。
  - `common`：35 个文件。
  - `planning`：43 个文件。
  - `proto_convert`：13 个文件。
- [x] 明确排除禁止内容：
  - 未复制 `proto/**/*.pb.*`。
  - 未复制 ROS2、demo、out、launch 或历史构建产物。
  - 未复制完整 `parking_algorithm_standalone` 目录。
- [x] 修改 `applications/source/valet_parking/CMakeLists.txt`：
  - `STANDALONE_PARKING_ROOT` 改为 `${CMAKE_CURRENT_SOURCE_DIR}/algorithm/parking_algorithm_standalone`。
  - 构建不再引用外部 `E:\APA\DDS\parking_algorithm_standalone`。

---

## 2. 重要边界

本阶段只是“源码位置本地化”，不是扩大算法功能。

本阶段没有接入：

- 完整 `OpenSpacePathProvider` 大类。
- 完整线程管理。
- NLP smoother。
- 完整 `Frame/DependencyInjector`。
- 真实车端 DDS Topic 契约。
- m57 板端运行闭环。

`proto/planning/sl_boundary.pb.h` 没有从 standalone 复制；当前继续使用 `src/compat/proto/planning/sl_boundary.pb.h` 兼容头，避免把 standalone 的 Protobuf 生成代码带入产品构建链路。

m57 仍只完成交叉编译、ELF 和依赖检查，尚未板端运行验证，不能写成板端通过。

---

## 3. 本次验证命令

x86 构建：

```bash
cd /mnt/e/APA/DDS/feature_integration/feature_integration_workspace
bash applications/source/valet_parking_tools/build_valet_parking.sh \
  --platform x86 \
  --out-dir /mnt/e/APA/DDS/feature_integration/feature_integration_workspace/out/valet_parking_algorithm_localized_032
```

x86 smoke 回归：

```bash
# default valid SelectedSlot
--domain-id 219

# parking_seq/path_id update regression
--domain-id 220 --slot-mode parking-seq-changes --interval-ms 650

# all-valid aux input regression
--domain-id 221 --with-aux-inputs --aux-mode all-valid
```

m57 交叉编译：

```bash
cd /mnt/e/APA/DDS/feature_integration/feature_integration_workspace
bash applications/source/valet_parking_tools/build_valet_parking.sh \
  --platform m57 \
  --jobs 8 \
  --out-dir /mnt/e/APA/DDS/feature_integration/feature_integration_workspace/out/valet_parking_algorithm_localized_032
```

---

## 4. 验收结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| 本地算法源码目录创建 | PASS | `applications/source/valet_parking/algorithm/parking_algorithm_standalone` |
| 复制文件数量 | PASS | 91 个文件 |
| 禁止内容排除 | PASS | 未复制 `proto/**/*.pb.*`、ROS2、demo、out、launch |
| CMake 外部路径解除 | PASS | `STANDALONE_PARKING_ROOT` 指向 `${CMAKE_CURRENT_SOURCE_DIR}/algorithm/parking_algorithm_standalone` |
| x86 构建 | PASS | `out/valet_parking_algorithm_localized_032/valet_parking_mvp/x86/lib/libvalet_parking.so` |
| x86 默认 valid 回归 | PASS | `domain_219_20260728_011828_723` |
| x86 `parking-seq-changes` 回归 | PASS | `domain_220_20260728_011828_726` |
| x86 all-valid 辅助输入回归 | PASS | `domain_221_20260728_011828_722` |
| m57 `libvalet_parking.so` 生成 | PASS | `out/valet_parking_algorithm_localized_032/valet_parking_mvp/m57/lib/libvalet_parking.so` |
| m57 ELF 架构 | PASS | `ELF 64-bit LSB shared object, ARM aarch64` |
| m57 MagnaDDS 依赖 | PASS | `libmagna-dds-core.so.1`、`libmagna-dds-impl.so` |
| m57 板端运行 | NOT_RUN | 当前无 m57 板端 |

---

## 5. 本阶段修改文件

- `applications/source/valet_parking/CMakeLists.txt`
- `applications/source/valet_parking/algorithm/parking_algorithm_standalone/**`
- `applications/source/valet_parking/README.md`
- `applications/source/Docs/valet_parking_magnadds/STATUS.yaml`
- `applications/source/Docs/valet_parking_magnadds/status_snapshots/032_项目状态快照_泊车算法源码本地化.md`

---

## 6. 下一步

进入 NEXT-033：继续沿用简化 DDS Topic，验证 SelectedSlot 多车位列表中 `opt_parking_seq` 选择不同 `ParkingLot` 的行为。构造同一消息内包含多个 `ParkingLot` 的样本，确认 `opt_parking_seq` 切换时 adapter 选择对应车位并触发 `TARGET_UPDATE`/重新生成路径，稳定后回到 `history=reused`。

仍不引入完整 `Frame/DependencyInjector`、线程管理、NLP smoother 或真实车端 Topic 协议。
