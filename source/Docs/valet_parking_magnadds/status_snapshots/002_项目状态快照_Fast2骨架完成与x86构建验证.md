# 项目状态快照 002：Fast-2骨架完成与x86构建验证

- 快照编号：002
- 日期：2026-07-26
- 当前阶段：Fast-2 — `.so` 与 mock工具骨架
- 阶段状态：IN_PROGRESS
- 创建原因：已完成最小代码骨架并完成 x86 白名单构建验证，需要记录当前阻塞与唯一下一步。

---

## 1. 本次完成事项

- [x] 新增 `applications/source/valet_parking/`：`CMakeLists.txt`、C API 头文件、组件与 C API 实现骨架。
- [x] 新增 `applications/source/valet_parking_tools/` 三工具模块：
  - `valet_parking_runner`
  - `selected_slot_mock_publisher`
  - `planning_trajectory_mock_subscriber`
- [x] 新增 `applications/config/valet_parking_mvp_bom.yaml`（只包含四个MVP模块）。
- [x] 使用 BOM 在 x86 完成构建与安装验证，产物包含：
  - `lib/libvalet_parking.so`
  - `app/valet_parking_runner`
  - `app/selected_slot_mock_publisher`
  - `app/planning_trajectory_mock_subscriber`
- [x] `nm -D` 已确认导出 C API：`valet_parking_create/start/stop/destroy/get_last_error/get_api_version`。

---

## 2. 当前阻塞项

| 编号 | 状态 | 描述 | 负责人 |
|---|---|---|---|
| B-001 | KNOWN_LIMITATION | 当前无 m57 板端，runtime 验证后置 | user/project |
| B-002 | OPEN | m57 交叉编译工具链目录不存在：`/usr/local/ARM-toolchain/gcc-arm-9.2-2019.12-x86_64-aarch64-none-linux-gnu` | env/workstation |

---

## 3. 当前验收状态（MVP门禁）

| 验收项 | 状态 | 说明 |
|---|---|---|
| `libvalet_parking.so` 可编译产出 | PASS(x86) / BLOCKED(m57) | x86 已产出；m57 因工具链缺失阻塞 |
| `/selected_slot` 订阅打通 | PENDING | 当前为骨架占位，未接入真实 DDS Reader |
| `/planning/trajectory` 发布打通 | PENDING | 当前为骨架占位，未接入真实 DDS Writer |
| MVP BOM 白名单构建 | PASS | 仅构建四个目标，未混入 `math/sort` |
| 最小交接可用 | PASS | `STATUS.yaml` + 快照持续更新 |

---

## 4. 下一步唯一动作

> 继续 Fast-2：在 `valet_parking` 中接入真实 MagnaDDS 最小 Reader/Writer 闭环；并在补齐 m57 工具链后重跑 m57 BOM 构建。

---

## 5. 当前禁止动作

- 禁止把 x86 通过写成 m57 通过。
- 禁止修改 `compile/` 与 `thirdparty/` 规避工具链问题。
- 禁止宣称已完成真实 DDS 通信（当前仍为骨架）。
- 禁止扩展到完整 Stage 状态机或完整算法迁移。
