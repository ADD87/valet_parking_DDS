# 项目状态快照 047_048：Topic 契约与 m57 联调包

日期：2026-07-29

## 本批次结论

`BATCH-047_048` 已完成。

本批次不继续扩展算法，也不改正式 IDL。目标是把两个容易混淆的边界写清楚：

```text
当前 DDS Topic/IDL = MVP 临时契约，不是车端最终协议
m57 交叉编译产物 + 运行包 = 可上板准备，不是板端 runtime PASS
```

## 完成内容

- 新增 `applications/source/Docs/valet_parking_magnadds/12_真实车端Topic契约差距表.md`
  - 梳理当前临时 Topic、IDL Type、生产者和消费者。
  - 明确接真实车端前必须确认 Domain、Topic name、Type name/IDL、QoS、字段顺序、枚举语义和重新生成代码。
  - 建议后续采用 `真实车端协议 -> protocol adapter/converter -> 当前内部结构` 的方式，避免真实协议变化直接污染算法内部。

- 新增 `applications/source/Docs/valet_parking_magnadds/13_m57板端运行包与联调Runbook.md`
  - 写清 WSL 打包命令、板端拷贝/解压/启动步骤、Topic 参数覆盖、禁用辅助 Topic 的方式。
  - 写清板端自检顺序：`file`、`readelf -d`、`ldd`、runner 启动、DDS discovery、write/take_next_sample。
  - 明确常见错误和验收边界，防止把“包已准备”误写成“板端已跑通”。

- 新增并验证 `applications/source/valet_parking_tools/package_valet_parking_m57.sh`
  - 默认输入：`out/valet_parking_batch_042_046/valet_parking_mvp/m57`
  - 默认输出：`out/valet_parking_m57_package/valet_parking_m57_runtime_<timestamp>`
  - 包内包含 runner、mock tools、`libvalet_parking.so`、`libmagna-dds-core.so*`、`libmagna-dds-impl.so`、启动脚本、README 和 manifest。
  - `PACKAGE_MANIFEST.txt` 记录包文件、ELF 架构、runner 动态依赖、`libvalet_parking.so` 动态依赖和 sha256。

## 验证结果

通过：

```text
bash -n applications/source/valet_parking_tools/package_valet_parking_m57.sh
bash applications/source/valet_parking_tools/package_valet_parking_m57.sh
```

产物：

```text
out/valet_parking_m57_package/valet_parking_m57_runtime_20260729_081921
out/valet_parking_m57_package/valet_parking_m57_runtime_20260729_081921.tar.gz
```

manifest 证据：

```text
valet_parking_runner: ELF 64-bit LSB executable, ARM aarch64
libvalet_parking.so: ELF 64-bit LSB shared object, ARM aarch64
libmagna-dds-core.so.1.0.0: ELF 64-bit LSB shared object, ARM aarch64
libmagna-dds-impl.so: ELF 64-bit LSB shared object, ARM aarch64
runner NEEDED: libvalet_parking.so, libmagna-dds-core.so.1, libmagna-dds-impl.so
libvalet_parking NEEDED: libmagna-dds-core.so.1, libmagna-dds-impl.so
RPATH: $ORIGIN:$ORIGIN/../lib:...
```

## 仍然不能声称完成的事情

- m57 板端 runtime 尚未验证。
- 真实车端 Topic/IDL/QoS 尚未确认。
- 当前 `parking_status` 仍只是 `replan_reason/estop.reason` 文本诊断，不是正式 DDS 字段。
- `PARKING_OUT_*` 仍是 `unsupported_in_mvp` 的安全停止分支，不是出库算法。
- 当前仍未接完整 FunctionManager、完整 Frame/DependencyInjector、完整 PreCheck 和 NLP smoother。

## 下一批

建议进入 `BATCH-049_052`：

```text
NEXT-049：临时 DDS sample 到内部模型的 protocol adapter 切口梳理
NEXT-050：SelectedSlot / ParkingCommand / Aux 输入快照和回放验证
NEXT-051：PlanningTrajectory 输出契约解析与对比脚本
NEXT-052：在简化 Topic 下形成端到端 I/O 回归矩阵
```
