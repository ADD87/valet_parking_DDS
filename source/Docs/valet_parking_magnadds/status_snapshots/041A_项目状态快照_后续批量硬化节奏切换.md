# 项目状态快照 041A：后续批量硬化节奏切换

## 阶段结论

已按用户要求修改后续计划：从 NEXT-042 开始，执行节奏切换为“先批量检测和批量修订，再统一编译 smoke，再集中排错”。

这不是扩大功能范围，而是调整执行方法，减少在 direct、command、stage/status 这类相互关联问题上的重复小步调试。

## 新节奏

```text
先批量静态扫描和契约检查
-> 再批量修订代码/脚本/文档
-> 再统一跑 x86 + m57 编译
-> 再统一跑 x86 smoke 矩阵
-> 再集中排查失败项
-> 再补充必要二次修订
-> 再更新 STATUS.yaml、状态快照和决策记录
-> 再提交并推送 applications Git
```

## 合并批次

原计划中的 NEXT-042 到 NEXT-046 合并为 `BATCH-042_046`：

```text
NEXT-042 direct 分支输出契约补强
NEXT-043 ParkingCommand 生命周期硬化
NEXT-044 PAUSE/BRAKE/FINISH 与原 Stage 差距收敛
NEXT-045 PARKING_OUT_* MVP 行为定义
NEXT-046 主链路 Stage 状态输出增强
```

## 保持边界

```text
不修改 compile/
不修改 thirdparty/
不扩大临时 IDL 字段
不把 replan_reason/estop.reason 文本诊断说成正式 DDS 字段
不把 m57 交叉编译说成板端运行通过
不全量复制外部 parking_algorithm_standalone 或 Repair_... 目录
```

## 下一步

进入 `BATCH-042_046`。

第一步先做批量静态扫描，输出 direct、command、stage/status、parking-out、smoke 脚本中的不一致点清单；第二步再批量修订。完成修订后统一跑 x86/m57 build 与 x86 smoke 矩阵。
