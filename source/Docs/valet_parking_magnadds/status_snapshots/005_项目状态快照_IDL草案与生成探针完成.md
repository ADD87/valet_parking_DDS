# 项目状态快照 005：IDL草案与生成探针完成

- 快照编号：005
- 日期：2026-07-26
- 当前阶段：Fast-2 — `.so` 与真实 DDS 最小闭环
- 阶段状态：IN_PROGRESS
- 创建原因：完成正式 IDL 草案、SDK `idlparser` 生成探针和生成代码语法编译检查；按用户要求先报告再继续后续 C++ 接线。

---

## 1. 本次完成事项

- [x] 新增 `applications/source/valet_parking/idl/valet_parking_topics.idl`。
- [x] IDL 覆盖 MVP 两个 Topic：`/selected_slot` 与 `/planning/trajectory`。
- [x] 输入 Topic 保留 `ParkingLotOutArray` MVP 字段：`header/count/loc_seq/opt_parking_seq/parking_lots/is_valid/path_point_size/traced_path/hpp_cruising_to_parking/enable_trigger`。
- [x] 输出 Topic 保留 `ADCTrajectory` allowlist 字段：`header/total_path_length/total_path_time/trajectory_point/is_replan/replan_type/replan_reason/longitudinal_diff/lateral_diff/gear/estop/trajectory_type`。
- [x] 已补齐 `PROTO_GAP_LIST.md` 要求的 `lane_id`、`area_probability`、`theta_a` 字段。
- [x] 固定 `gear` 与 `trajectory_type` 的枚举顺序，使数值从 0 起与原 proto 对齐。
- [x] 使用 SDK `idlparser` 生成 C++ 类型与 TopicDataType 文件成功。
- [x] 对生成的 `.cpp` 使用 workspace x86 MagnaDDS 头文件执行 `g++ -std=c++14 -fsyntax-only`，结果通过。

---

## 2. 本次未完成事项

- [ ] 尚未把生成类型接入 `valet_parking/CMakeLists.txt`。
- [ ] 尚未把 `valet_parking` 内部 Reader/Writer 从 `TopicDataType_raw` 替换为 `SelectedSlotTopicDataType` 和 `PlanningTrajectoryTopicDataType`。
- [ ] 尚未把 mock publisher/subscriber 从字符串 payload 迁移到正式 IDL 类型。
- [ ] 尚未重新执行 x86 端到端运行验证。
- [ ] 尚未执行 m57 BOM 构建；当前仍被交叉工具链和 m57 MagnaDDS 实库问题阻塞。

---

## 3. 当前交付物

| 文件/产物 | 状态 | 说明 |
|---|---|---|
| `applications/source/valet_parking/idl/valet_parking_topics.idl` | 已创建 | 正式 DDS 线协议源 |
| `applications/source/valet_parking/generated/valet_parking_topics.h` | 已生成 | SDK `idlparser` 生成类型声明 |
| `applications/source/valet_parking/generated/valet_parking_topics.cpp` | 已生成 | SDK `idlparser` 生成类型实现 |
| `applications/source/valet_parking/generated/valet_parking_topicsTopicDataType.h` | 已生成 | SDK `idlparser` 生成 TopicDataType 声明 |
| `applications/source/valet_parking/generated/valet_parking_topicsTopicDataType.cpp` | 已生成 | SDK `idlparser` 生成 TopicDataType 实现 |

---

## 4. 最近执行动作或命令

| 时间 | 动作/命令 | 结果 | 日志/证据 |
|---|---|---|---|
| 2026-07-26 | `idlparser ../idl/valet_parking_topics.idl` | PASS | grammar check passed; Type Files and TopicDataType Files generated successfully |
| 2026-07-26 | `g++ -std=c++14 ... -fsyntax-only generated/valet_parking_topics.cpp` | PASS | 无编译错误输出 |
| 2026-07-26 | `g++ -std=c++14 ... -fsyntax-only generated/valet_parking_topicsTopicDataType.cpp` | PASS | 无编译错误输出 |

---

## 5. 当前阻塞项

| 编号 | 状态 | 描述 | 负责人 | 下一步 |
|---|---|---|---|---|
| B-001 | OPEN | 当前无 m57 板端，运行态通信验收暂无法闭环 | user/project | 板端可用后执行 runtime 验证 |
| B-002 | OPEN | 当前 WSL 中标准 m57 工具链目录不存在，且未找到真实 `aarch64-none-linux-gnu-gcc` | env/workstation | 安装/恢复工具链到计划路径，或开 DR 评估工具链路径调整 |
| B-003 | OPEN | `thirdparty/m57/magnadds/lib` 疑似缺少真实 m57 MagnaDDS 实库，仅发现 0 字节 `.so` 占位文件 | env/workstation | 补齐 m57 MagnaDDS `libmagna-dds-core.so.1.0.0` 与 `libmagna-dds-impl.so` 等实库后再验收 |

---

## 6. 阶段验收清单

| 验收项 | 结果 | 证据/备注 |
|---|---|---|
| `valet_parking_topics.idl` 存在 | PASS | `applications/source/valet_parking/idl/valet_parking_topics.idl` |
| IDL 覆盖两个 MVP Topic | PASS | `SelectedSlot` 与 `PlanningTrajectory` |
| SDK `idlparser` 语法检查 | PASS | 生成日志显示 grammar check passed |
| 生成 `SelectedSlotTopicDataType` | PASS | `generated/valet_parking_topicsTopicDataType.h` |
| 生成 `PlanningTrajectoryTopicDataType` | PASS | `generated/valet_parking_topicsTopicDataType.h` |
| 生成代码语法编译 | PASS(x86 headers) | `g++ -fsyntax-only` 通过 |
| C++ DDS Reader/Writer 正式接线 | PENDING | 下一步执行，需用户确认 |
| m57 静态构建 | BLOCKED | 缺交叉编译器与 m57 MagnaDDS 实库 |

---

## 7. 变更文件列表

| 文件 | 类型 | 说明 |
|---|---|---|
| `applications/source/valet_parking/idl/valet_parking_topics.idl` | 新增 | 两 Topic 的正式 IDL 源 |
| `applications/source/valet_parking/generated/valet_parking_topics.h` | 新增 | 生成类型声明 |
| `applications/source/valet_parking/generated/valet_parking_topics.cpp` | 新增 | 生成类型实现 |
| `applications/source/valet_parking/generated/valet_parking_topicsTopicDataType.h` | 新增 | 生成 TopicDataType 声明 |
| `applications/source/valet_parking/generated/valet_parking_topicsTopicDataType.cpp` | 新增 | 生成 TopicDataType 实现 |
| `applications/source/Docs/valet_parking_magnadds/STATUS.yaml` | 修改 | 更新当前完成状态和下一步 |
| `applications/source/Docs/valet_parking_magnadds/status_snapshots/005_项目状态快照_IDL草案与生成探针完成.md` | 新增 | 本阶段状态快照 |

---

## 8. 决策记录

| 决策编号 | 文件 | 状态 |
|---|---|---|
| DR-000 | `applications/source/Docs/valet_parking_magnadds/decision_records/DR-000_初始范围锁定.md` | ACTIVE |
| DR-001 | `applications/source/Docs/valet_parking_magnadds/decision_records/DR-001_Proto处理与proto_convert复用策略.md` | ACTIVE |
| DR-002 | `applications/source/Docs/valet_parking_magnadds/decision_records/DR-002_MVP优先级重排_先跑通再硬化.md` | ACTIVE |

---

## 9. 下一步唯一动作

> 等待用户确认；确认后将 generated `SelectedSlot` / `PlanningTrajectory` 接入 CMake 和 C++ Reader/Writer，替换当前 raw payload 的 x86 通信路径。

---

## 10. 当前禁止动作

- 禁止把 IDL 生成通过写成 DDS 类型通信已经通过。
- 禁止把 x86 通过写成 m57 通过。
- 禁止在无板端情况下写 runtime 全通过结论。
- 禁止修改 `compile/` 或 `thirdparty/` 绕过工具链问题。
- 禁止擅自扩展到完整 Stage 状态机或完整算法迁移。

---

## 11. 给下一个 AI 的一句话

当前处于 Fast-2，正式 IDL 和生成代码探针已通过，但 C++ 仍在 raw payload 路径；等待用户确认后再把 generated 类型接入 Reader/Writer 并重跑 x86 闭环。

