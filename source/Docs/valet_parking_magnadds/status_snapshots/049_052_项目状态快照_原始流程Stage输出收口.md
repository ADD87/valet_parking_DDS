# 项目状态快照 049_052：原始流程 Stage 输出收口

日期：2026-07-29

## 本批次结论

`BATCH-049_052` 已完成。

本批次根据用户纠偏，把执行重心从外围 Topic/打包问题拉回主线：对照 `00_ValetParkingStageParking_超详细流程图总览_重要.md`，减少当前 MagnaDDS Adapter 与原始 `ValetParkingStageParking` 的行为差异。

本批次收敛的核心差异是原始流程里的“对外状态翻译与覆写链”：

```text
PathPartition finish/destination
SpeedOptimizer interactive stage
Stage publishable trajectory + target_gear + parking_status
finish 覆写 interactive stage
```

## 代码变更

- `applications/source/valet_parking/src/valet_parking_stage_parking_adapter.cpp`
  - 新增 `PathDecisionToString()`、`FinishStatusToString()`、`StageParkingStatus()`、`StageTrajectoryType()`。
  - 新增 `BuildOpenSpaceStageOutputContract()`，生成 `STAGE_OUTPUT open_space` 诊断契约。
  - `RunPathPartition()` 输出 `decision_name/finish_name/destination_reached`。
  - `RunSpeedOptimizer()` 输出 `stage_name=INIT/RUNNING/WAITOBSTACLE/WAITREPLAN`。
  - 普通主链成功后把 `STAGE_OUTPUT` 插入 `replan_reason/estop.reason`。
  - 速度优化失败但 PathPartition 可回退时也输出 `STAGE_OUTPUT`，并标注 `speed_optimizer=fallback`。

- `applications/source/valet_parking_tools/smoke_valet_parking_x86.sh`
  - valid 场景新增普通主链 Stage 输出契约断言。
  - 要求日志中可见 `PATH_PARTITION decision_name/finish_name/destination_reached`、`SPEED_OPTIMIZER stage_name` 和 `STAGE_OUTPUT`。

- `applications/source/Docs/valet_parking_magnadds/05_TempAPA原始流程复现差距对照表.md`
  - 更新 PathPartition、SpeedOptimizer、Stage 发布轨迹、IsReadyToFinishStage 的差异状态。
  - 记录 BATCH-049_052 具体减少了哪些差异。

- `applications/source/Docs/valet_parking_magnadds/14_原始流程差异收敛执行记录.md`
  - 新增专门文档，固定后续执行方向：继续围绕原始流程图减少差异。

## 验证结果

通过：

```text
bash applications/source/valet_parking_tools/build_valet_parking.sh --out-dir out/valet_parking_flow_gap_049_052
bash applications/source/valet_parking_tools/build_valet_parking.sh --platform m57 --out-dir out/valet_parking_flow_gap_049_052
bash applications/source/valet_parking_tools/smoke_valet_parking_x86.sh --run-root out/valet_parking_flow_gap_049_052/valet_parking_mvp/x86 --domain-id 190 --count 3 --interval-ms 500 --expect-thread-provider-stop
bash applications/source/valet_parking_tools/smoke_valet_parking_batch_042_046.sh --run-root out/valet_parking_flow_gap_049_052/valet_parking_mvp/x86 --first-domain-id 191 --timeout-ms 25000
```

产物：

```text
out/valet_parking_flow_gap_049_052/valet_parking_mvp/x86/lib/libvalet_parking.so
out/valet_parking_flow_gap_049_052/valet_parking_mvp/m57/lib/libvalet_parking.so
```

关键日志：

```text
domain_190_20260729_084957_750
domain_191_20260729_085214_739
domain_206_20260729_085423_2522
```

## 仍然不能声称完成的事情

- 不能声称完整复现原始 `ValetParkingStageParking`。
- `parking_status` 仍是临时诊断文本，不是正式 DDS 字段。
- `FunctionManager`、`sys_mode`、`next_stage_`、`FinishScenario()` 仍未完整接入。
- `Frame/OpenSpaceInfo/PlanningContext` 仍是轻量替代。
- `OpenSpacePathSmoother/NLP` 仍未接入。
- m57 仍未板端 runtime 验证。

## 下一批

进入 `BATCH-053_056`：

```text
NEXT-053：IsReadyToFinishStage 轻量状态机
NEXT-054：FunctionManager 输入映射表和当前临时输入对齐
NEXT-055：Frame/OpenSpaceInfo 最小字段契约
NEXT-056：PreCheck / collision / wheel mask 差异收敛
```
