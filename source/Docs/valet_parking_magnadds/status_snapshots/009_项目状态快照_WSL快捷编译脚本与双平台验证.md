# 项目状态快照 009：WSL快捷编译脚本与双平台验证

- 快照编号：009
- 日期：2026-07-26
- 当前阶段：Fast-2 - `.so` + typed DDS + ROI_DECIDER/PATH_PROVIDER 最小闭环
- 阶段状态：PASS_QUICK_BUILD_X86_M57_BLOCKED_BOARD
- 创建原因：用户要求确认当前代码是否能链接 MagnaDDS，并便捷编译为 x86/m57 平台的 `libvalet_parking.so`；本阶段新增 WSL 快捷编译脚本并完成双平台验证。

---

## 1. 本次完成事项

- [x] 新增快捷编译脚本：
  - `applications/source/valet_parking_tools/build_valet_parking.sh`
- [x] 脚本默认构建 x86 和 m57 两个平台。
- [x] 脚本支持选择平台、构建模式、并行任务数和输出目录。
- [x] 脚本构建后自动检查：
  - `libvalet_parking.so` 是否生成
  - ELF 架构是否符合目标平台
  - `readelf -d` 是否能看到 MagnaDDS 动态依赖
- [x] 更新 `applications/source/valet_parking/README.md`，记录 WSL 使用方法。

---

## 2. 快捷命令

在 WSL Ubuntu 中执行：

```bash
cd /mnt/e/APA/DDS/feature_integration/feature_integration_workspace
bash applications/source/valet_parking_tools/build_valet_parking.sh
```

只编 x86：

```bash
bash applications/source/valet_parking_tools/build_valet_parking.sh --platform x86
```

只编 m57：

```bash
bash applications/source/valet_parking_tools/build_valet_parking.sh --platform m57 --jobs 8
```

指定输出目录：

```bash
bash applications/source/valet_parking_tools/build_valet_parking.sh \
  --platform both \
  --out-dir /mnt/e/APA/DDS/feature_integration/feature_integration_workspace/out/valet_parking_quick_build
```

---

## 3. 本次验证命令

```bash
cd /mnt/e/APA/DDS/feature_integration/feature_integration_workspace
bash applications/source/valet_parking_tools/build_valet_parking.sh \
  --platform both \
  --jobs 8 \
  --out-dir /mnt/e/APA/DDS/feature_integration/feature_integration_workspace/out/valet_parking_quick_build_009
```

---

## 4. 验收结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| x86 `libvalet_parking.so` 生成 | PASS | `out/valet_parking_quick_build_009/valet_parking_mvp/x86/lib/libvalet_parking.so` |
| x86 ELF 架构 | PASS | `ELF 64-bit LSB shared object, x86-64` |
| x86 MagnaDDS 依赖 | PASS | `readelf -d` 显示 `libmagna-dds-core.so.1` |
| m57 `libvalet_parking.so` 生成 | PASS | `out/valet_parking_quick_build_009/valet_parking_mvp/m57/lib/libvalet_parking.so` |
| m57 ELF 架构 | PASS | `ELF 64-bit LSB shared object, ARM aarch64` |
| m57 MagnaDDS 依赖 | PASS | `readelf -d` 显示 `libmagna-dds-core.so.1` 和 `libmagna-dds-impl.so` |
| m57 板端运行 | NOT_RUN | 当前无 m57 板端 |

---

## 5. 边界说明

本阶段证明的是：

- 当前泊车适配代码可以链接当前 workspace 的 MagnaDDS 库。
- x86 和 m57 都能生成 `libvalet_parking.so`。
- m57 产物是 AArch64 ELF。

本阶段不证明：

- m57 产物已经在真实板端运行成功。
- 板端 DDS Domain、网络、Topic、库路径配置已经通过。
- `PATH_PROVIDER` 已接入。该项进入下一阶段执行。

---

## 6. 下一步

继续接入 `PATH_PROVIDER`，保持当前结构：

```text
SelectedSlot -> ROI_DECIDER -> PATH_PROVIDER -> PlanningTrajectory
```

本阶段的 PATH_PROVIDER 接入策略是先使用 standalone 中已经独立化的 `OpenSpacePathGenerator` 粗路径生成能力，不把完整 `OpenSpacePathProvider` 的 Stage/Frame/DependencyInjector 依赖一次性搬进 DDS 侧。

