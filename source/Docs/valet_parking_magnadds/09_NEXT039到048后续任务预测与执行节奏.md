# NEXT-039 到 NEXT-048 后续任务预测与执行节奏

本文用于记录 NEXT-038 之后的连续任务预测。它不是新的大计划替代品，而是当前状态下的 10 步执行路线，防止后续工作在调试细节里绕圈。

## 固定执行节奏

每个 NEXT 阶段都按这个顺序执行：

```text
先修订代码/脚本/文档
-> 再编译
-> 再跑 x86 smoke
-> 再排查失败
-> 再更新 STATUS.yaml 和状态快照
-> 再提交并推送 applications Git
```

也就是说，除非编译直接阻塞到无法继续，否则不要先陷入排查历史问题。先把本阶段想接入的能力清楚落地，再用验证来收口。

## 后续 10 个 NEXT

| NEXT | 主题 | 主要修订 | 验证重点 |
|---|---|---|---|
| NEXT-039 | OPEN_SPACE_STRAIGHT_PATH direct 分支硬化 | 让 `direct_speed_mps` 映射到速度优化器 speed bound；增加速度方向冲突 smoke；补 `target_gear / trajectory_type / parking_status` 差距说明 | x86 direct stop_path/direct_moving/velocity conflict smoke；m57 交叉编译 |
| NEXT-040 | OpenSpacePathProvider 线程体系接入 | 新增本地 `OpenSpacePathProvider` 与 `OpenSpaceThreadManager`；普通主链改走 TargetPlan；多车位非选中 lot 进入 PrePlan 候选；复验阶段补 PrePlan 队列硬化 | x86 valid/multi-lot/direct 回归；m57 交叉编译 |
| NEXT-041 | OpenSpacePathProvider 线程生命周期硬化 | 新增 early_stop/cancel，验证 stop/destructor、target timeout、PrePlan 候选复用；评估 NLP smoother 依赖并暂不接入产品链路 | x86 valid/timeout/multi-lot/direct smoke；m57 交叉编译 |
| NEXT-042 | direct 分支输出契约补强 | 在临时 IDL 不扩字段的前提下，把 direct 输出诊断字段标准化；明确哪些字段只是 replan_reason 文本，哪些是正式 DDS 字段 | subscriber 日志可稳定检索 direct 状态 |
| NEXT-043 | ParkingCommand 生命周期硬化 | 梳理 command 缓存、清除、reset_history、重复 command 和 SelectedSlot 时序；避免旧命令意外持续控制 | command 顺序 smoke、invalid clear、reset_history smoke |
| NEXT-044 | PAUSE/BRAKE/FINISH 与原 Stage 差距收敛 | 对比原始 `ValetParkingStageParking` 的暂停、制动、完成状态；补足当前可表达的 status/reason | x86 pause/brake/finish smoke 保持非 estop 停止 |
| NEXT-045 | PARKING_OUT_* MVP 行为定义 | 当前 parking out 类命令只返回 unsupported stop；下一步决定保持显式 unsupported，还是接入第一层最小退车/出库行为 | unsupported smoke 或最小 parking-out smoke |
| NEXT-046 | 主链路 Stage 状态输出增强 | 给普通 `ROI -> PATH_PROVIDER -> PATH_PARTITION -> SPEED` 链路补更稳定的 stage/status/replan 诊断文本 | valid、target update、obstacle update smoke |
| NEXT-047 | 真实车端 Topic 契约差距表 | 对照培训材料和真实车端可能 Topic，把临时 IDL 字段和正式协议缺口列清楚 | 文档验收，不贸然改 IDL |
| NEXT-048 | m57 板端运行包准备和联调 Runbook | 在不声称板端通过的前提下，整理 m57 运行所需 app/lib/环境变量/Topic/Domain 参数，并写清板端启动和定位步骤 | m57 包内容、ELF、NEEDED、RPATH 检查；板端结果仍待真实设备 |

## 当前判断

当前最专业的路线仍不是立刻搬完整 `Frame/DependencyInjector`，而是在已接入线程 Provider 骨架后，继续把已经跑通的链路做硬：

```text
SelectedSlot -> ROI_DECIDER -> PATH_PROVIDER -> PATH_PARTITION -> SPEED_OPTIMIZER
ParkingCommand DIRECT_* -> OPEN_SPACE_STRAIGHT_PATH -> SPEED_OPTIMIZER
```

原因很简单：这两条链路已经能 x86 DDS 闭环和 m57 交叉编译。NEXT-040 后，普通链路里的 `OpenSpacePathProvider / OpenSpaceThreadManager` 线程骨架也已进入构建和 smoke。后续应该优先验证线程生命周期、候选复用、输出契约和状态诊断，再判断是否接真正 NLP smoother 或完整 Stage 框架。

## 不能越界的点

- 不能把 `replan_reason` 文本里的 `parking_status` 写成正式 DDS 字段已经存在。
- 不能把 m57 交叉编译写成板端运行通过。
- 不能为了快而修改 `compile/`、`thirdparty/`。
- 不能全量复制外部 `parking_algorithm_standalone` 或 `Repair_...`。
- 不能把调试中的临时输出当作真实车端协议。
